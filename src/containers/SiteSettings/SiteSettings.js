import React, { Component } from 'react';
import Typography from '@material-ui/core/Typography';

class Dashboard extends Component {
    render() {
        return (
            <Typography variant="h1">Site Settings</Typography>
        )
    }
}

export default Dashboard;